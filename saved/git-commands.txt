#Adding things here I don't use often so I can reference
them when needed

squash multiple commits into one
--------------------------------
git rebase --root -i
fixup or squash each commit
i for editor mode
escape to exit editor mode
:x to save changes
git push origin <branch-name> --force